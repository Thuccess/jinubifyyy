# Render Blueprint – Jinubify (GitHub → Render)
# Deploy from repo root; creates two Web Services: backend API + Next.js frontend.
# Set secret env vars in Render Dashboard after first deploy (see RENDER_DEPLOY.md).
#
# Region: frankfurt lowers RTT vs Oregon for East Africa (Uganda/Kenya). Pair with MongoDB Atlas
# in eu-central-1 (Frankfurt) or af-south-1 (Johannesburg) — same region as API is best.

services:
  # Backend: Express API + MongoDB (use MongoDB Atlas; set MONGODB_URI + JWT_SECRET in Dashboard)
  - type: web
    name: jinubify-api
    region: frankfurt
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        fromService:
          type: web
          name: jinubify-web
          property: url
      # Optional: set in Dashboard if needed
      # - key: JWT_EXPIRES_IN
      #   value: 7d
      # - key: SMTP_HOST
      #   sync: false
      # - key: SMTP_USER
      #   sync: false
      # - key: SMTP_PASS
      #   sync: false
      # - key: CONTACT_EMAIL
      #   sync: false

  # Frontend: Next.js – must use "npm start" (next start), NOT "npm run dev"
  - type: web
    name: jinubify-web
    region: frankfurt
    runtime: node
    rootDir: frontend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        sync: false
      # Optional: for CDN or custom domain
      # - key: NEXT_PUBLIC_MEDIA_BASE_URL
      #   sync: false
      # - key: NEXT_PUBLIC_SITE_URL
      #   sync: false
