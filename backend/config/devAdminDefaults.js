/**
 * Local development defaults for the first admin user.
 * Override with DEV_ADMIN_EMAIL / DEV_ADMIN_PASSWORD in .env when needed.
 * Do not use these defaults in production.
 */
export const DEV_ADMIN_DEFAULT_EMAIL = 'admin@jinubify.com';
export const DEV_ADMIN_DEFAULT_PASSWORD = 'Q2#vL7$K!5rX@9ZpT3@m';
